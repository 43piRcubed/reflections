What happens when you initialize a repository? Why do you need to do it?

	this initializes reporsitory as a git repository. needed for git to recognize folder as a reporisotry and to be bale to issue git commands on files in repository.

How can you use the staging area to make sure you have one commit per logical change?
	
	comparing staging area file with latest commit of file in repository.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?
	
	different versions of a game: easy, difficult
	same game different language
	investigation of some new options:  keep regular version moving forward while investigating additional aspects in a different branch.

How do the diagrams help you visualize the branch structure?
    diagrams show master branch and branches of of it in different color

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
	merging two branches creates a new version that includes changes on each branch.  This makes it easy to combine work from different people/teams. the diagram makes clear where the branches a branching off and where they are merged. so it easy to follow the history.
